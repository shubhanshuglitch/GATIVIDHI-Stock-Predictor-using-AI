# Heroku Procfile
# This file tells Heroku how to run your app

# Backend
web: cd backend && npm start

# ML Service (if deploying on Heroku)
# web: cd ml-service && uvicorn app:app --host 0.0.0.0 --port $PORT
